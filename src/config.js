const BASE_URL = 'http://10.58.3.40:8000';
const API = {
  users: `${BASE_URL}/users`,
  products: `${BASE_URL}/products`,
  carts: `${BASE_URL}/carts`,
  oders: `${BASE_URL}/oders`,
};
export default API;
